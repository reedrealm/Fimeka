// kernel-inisial // 

.code 

     < build asset-kenel.jar 
           dir place-local 
            sol kotlin user code :: name ­­­­­­ 
             if sol asset-sys ::: .fonction
             start dir ./boot 
                if start %cm%
